"""
Desktop RSS Wall Helper — Python helper tools for the GNOME Shell extension.

Handles RSS fetching, image indexing, and cache management so the
GNOME Shell extension stays small and non-blocking.
"""

__version__ = "0.1.0"
